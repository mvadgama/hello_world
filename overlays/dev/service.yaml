apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  ports:
  - port: 81
    protocol: TCP
    targetPort: 81
    name: http
  type: LoadBalancer
  selector:
    app: nginx