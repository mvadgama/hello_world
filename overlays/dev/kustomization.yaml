apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

commonLabels:
  env: dev

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

namespace: default

patchesStrategicMerge:
  - deployment.yaml
